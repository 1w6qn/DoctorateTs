# 代码冗余审查（多余代码 / 多余测试 / 重复实现）

> 审查日期：2026-09-10 · 只读审查（未修改任何源码）
> 范围：`app/`（392 个 .ts / 147,215 行）、`tests/`（276 个 .ts / 64,595 行，其中 271 个 `*.test.ts` / ≈2,900 个 `it` / 7,609 个 `expect`）、`scripts/`（45 个 .ts / 13,008 行）
> 方法：脚本化机械取证（token 窗口克隆检测、导出/导入图分析、未用导入检测、测试↔源码映射、路由重叠分析）+ 逐条人工复核（含 3 个并行子审区）
> 复跑脚本：`tmp/audit/*.mjs`（gitignored，见文末附录）

## 与既有文档的关系

`docs/重复实现审查-整合清单.md`（2026-09-09）已完成批次 1、批次 2。本报告是**复核 + 增量**：既确认了旧清单尚未落地的项（第三节标注），也给出旧清单未覆盖的新证据（未用导入 3,966 处、死导出 774 处、重复测试文件、测试镜像腐化等）。

---

## 速览

| 维度 | 数量 | 说明 |
|---|---|---|
| 字节级重复的实现（P0 级） | 5 组 / ≈350 行 | 其中 4 组是**已确认死代码**（删除即收益） |
| 复制粘贴导致的**行为分叉** | 2 处 | 1 处安全相关（商店负数量购买），1 处响应字段缺失 |
| 模板级重复 import 块 | 30 文件 / 3,844 行 | 其中 3,588 个导入绑定**从未被使用** |
| 零引用文件 | 11 个 | 含 1 个 0 字节文件、1 个纯注释文件、5 个 barrel、2 个占位 |
| 零引用导出 | 774 个 | 483 个业务导出 + 291 个 excel 防腐层 re-export（后者为有意设计） |
| 未使用 import 绑定 | 4,001 个 | app 3,966（3,588 在 activities）+ tests 35 |
| 孤儿 zod schema | 18 个 | 其中 building 15 个恰好对应 15 条未校验 POST 路由 |
| 重复测试文件 | 1 个（535 行 / 37 用例跑两遍） | `tests/unit/{game/,}model/events.test.ts` |
| 测试内重复脚手架 | excel 门面 122 文件 / 4,762 行；`makePlayer` 57 / 1,066 行；`mockRes` 48 / 341 行 | `tests/helpers/mockExcel.ts` 等 4 个 helper **零使用** |
| 自证式测试（不碰 app 代码） | 3 文件 / 1,556 行 | `tests/unit/model/{character,playerdata,battle}.test.ts` |

---

## 一、重复实现

### 🔴 P0-1 `interlockRefresh/logic.ts` 私有副本：184 行字节级重复，且不可达

`app/game/modules/activities/interlockRefresh/logic.ts:150-329` 复制了 `app/game/modules/activities/shared/shared.ts` 已导出的 5 个辅助函数：

| 函数 | shared/shared.ts | interlockRefresh/logic.ts | 行数 |
|---|---|---|---|
| `confirmOneActivityMission` | 167-214 | 152-199 | 48 |
| `autoConfirmActivityMissionsIn` | 220-252 | 201-233 | 33 |
| `collectRawBody` | 254-261 | 235-242 | 8 |
| `arkhubFullHost` | 269-273 | 244-248 | 5 |
| `ItemTypeToString` | 282-371 | 250-339 | 90 |

去注释/空白后逐字符比对，5 个函数体的唯一差异是 `export ` 关键字本身（长度差恒为 7）。

**并且是死代码**：该文件唯一导出 `handleInterlockrefreshSquad`（`logic.ts:146-148`）只有 `return player.delta`，不调用任何一个 helper；5 个 helper 只在彼此之间互相调用。`interlockRefresh/router.ts:140` 仅 import 了那个导出函数。全仓无其他引用。

> 修复：删除 `interlockRefresh/logic.ts:150-329`，改用 `import { ... } from "../shared/shared"`，并顺手清掉随之失效的 `ItemBundle/ItemType/excel/logger/config/ReqSchema` 导入（该文件 127 个导入绑定中 124 个未使用）。

### 🔴 P0-2 `home/routes.ts` 重复实现了 charRotation 的 4 个端点（类型+schema+路由三重重复）

同一功能在两个模块各写了一份，且 home 侧**被挂载顺序遮蔽为死代码**：

| 重复物 | 位置 | 状态 |
|---|---|---|
| 4 条路由 | `home/routes.ts:76-101` ≡ `character/charRotation.routes.ts:36-80`（其中 3 条逐字相同，`createPreset` 已分叉） | home 侧永不生效 |
| 4 个 zod schema | `home/home.schema.ts:23-43`（`charRotation*Schema`） ≡ `character/charRotation.schema.ts:17-40` | **逐字重复** |
| 协议类型 | `home/routes.ts:32-41` 跨模块 import `../character/charRotation` | 违反模块边界，靠白名单放行 |

- 遮蔽证据：`app/game/routes.ts:111` 先以 `prefix:"/charRotation"` 挂载 `character/charRotation.routes`，`routes.ts:126` 才以 `prefix:"/"` 挂载 home；文件头注明「数组有序：顺序即挂载顺序」。
- 白名单证据：`tests/unit/architecture/module-boundary.test.ts:86` 有一条技术债条目 `{ file: "app/game/modules/home/routes.ts", spec: "../character/charRotation", reason: "共享角色轮换数据" }` —— 边界守卫正是为这份重复开的豁口。
- **两侧已分叉**：home 侧 `createPreset` 返回 `{ instId: await player.charRotation.createPreset(), ...player.delta }`（`home/routes.ts:85-88`），而**真正生效**的 `charRotation.routes.ts:48-52` 丢弃返回值；`charRotation.ts:23` 注释「服务端未返回 instId」与类型 `instId?: string`。两份实现谁对需要按客户端契约确认——这正是重复代码的代价（同文件 `home/routes.ts:151-153` 曾修过同类 `/charm/setSquad` 死路由，charRotation 这 4 条漏删）。

> 修复：删除 `home/routes.ts:76-101` 与 `home/home.schema.ts:23-43`，移除 `routes.ts:32-41` 的跨模块 import 与 `module-boundary.test.ts:86` 白名单条目；`createPreset` 是否需要回 `instId` 单独定案。

### 🔴 P0-3 `shop/logic/fes.ts#buyLMTGSGood` 漏掉 `_assertBuyCount`（复制导致的单向修复遗漏）

`_assertBuyCount`（`shop/logic/misc.ts:21-25`）的文档写明用途：**没有它时负数 count 会让价格/发放数量取反，免费刷货币**。6 个 `buy*` 中有 5 个在调用后加了这道守卫，唯独 `buyLMTGSGood` 漏了：

```
fes.ts:47   buyEPGSGood        有
fes.ts:88   buyREPGood         有
fes.ts:128  buyClassicGood     有
fes.ts:207  buyLMTGSGood       ← 缺（仅剩 221 _assertAffordable / 223 _assertAvail）
low-high.ts:63/105/185         有
```

`_assertAffordable` 在 `count <= 0` 时直接 return（`misc.ts:64`），`_assertAvail` 只查上界（`misc.ts:99`），入口 schema 也只写 `count: z.number()`（`shop/schemas.ts:69-72`，不校验正数），因此负数量会一路走到 `fes.ts:235-237` 的 `items:use`（`count: good.price.count * count`）。路由 `/shop/buyLMTGSGood` 在线（`shop/handler.ts:715-723`）。

> 修复：在 `fes.ts:221` 之前补 `mgr._assertBuyCount(count);`，并加一条负数量单测。
> 诚实说明：负数量最终能否真的反向入账取决于 `items:use → _useItem` 对负数的处理（`misc.ts:17-18` 的注释明确写了「负数 count 使价格/发放数量取反……反向入账」），本报告只做到静态证据链，**建议按批次 C 第 1 项补一条端到端负数量测试来确认可利用性**，而不是直接当作已确认漏洞。

### 🔴 P0-4 `arkhub/arkpixel.ts` 残留的 multipart 解析副本（31 行，已有共享版且已在用）

`arkpixel.ts:262-297` 的 `parseMultipartForm` 与 `kernel/util/multipart.ts:7-37` 逐字相同（该 helper 的 docstring 写着「由 arkhub 活动域上移，供多域复用」）。迁移已完成但旧副本没删：`user/routes.ts:8`、`arkhub/router.ts:14`、`arkhub/logic.ts:13`、`tests/unit/manager/activity-arkpixel.test.ts:9` 导入的都是 kernel 版，`arkpixel.ts` 这个导出**全仓零引用**。

### 🔴 P0-5 `bossRush/router.ts` 内嵌 enemyDuel 死副本（71 行 + 16 个类型 import）

`bossRush/router.ts:216-286` 是 `enemyDuel/router.ts:136-196` 的陈旧快照：4 个函数（`enemyDuelMatchState` :219 / `genEnemyDuelId` :222 / `enemyDuelServerAddress` :233 / `buildEnemyDuelFinishResponse` :238）在 bossRush 内**零调用**——这 4 个名字在文件中只出现在定义行，且全文件的路由注册只到 `:206`（`router.post` 行号 137/162/186/206），之后直到 `:287 export default router` 再无路由；末尾 `:281-286` 甚至留着一段失去函数体的孤儿 JSDoc（`@route POST /activity/enemyDuel/singleBattleStart`）。同时 `bossRush/router.ts:57-72` 的 16 个 `EnemyDuel*` 类型 import 只服务于这些死函数。

### 🟠 P1-1 activities：30 个文件重复 128 行「类型总导入」

`app/game/modules/activities/` 下 30 个 `router.ts`/`logic.ts` 以逐字相同的方式 `import { ...约124个类型... } from "../shared/activity"`：

- 30 个文件的该 import 块合计 **3,844 行**（最大 `arkhub/router.ts` 143 行，`arkhub/logic.ts` 142 行）
- 其中 **3,588 个导入绑定在该文件内从未被引用**（例：`act25side/router.ts:5-131` 导入了 `Act24sideAlchemy*`、`BossRush*`、`EnemyDuel*` 等约 120 个类型，正文只用到 2 个）
- 全 app 未使用导入绑定 3,966 个，其中 3,588（90.5%）集中在这 30 个文件
- 后果：任何一次 barrel 改名都要改 30 处（`shared/activity.ts` 845 行），且 `act25side/router.ts` 这类「全文只有 import + 2 个 handler」的文件体积被夸大到 230 行

> 修复：按文件实际使用裁剪 import；中期把 `shared/activity.ts` 的巨型 barrel 按活动族拆分（或在 `activities/shared/` 增加 `act25side.ts` 等按族类型模块）。机械改动可用 `eslint --fix`（规则 `@typescript-eslint/no-unused-vars` 已配置在 `eslint.config.mjs:9`，但 **package.json 没有 lint 脚本**，因此从未执行）。

### 🟠 P1-2 `shop/logic/fes.ts` ↔ `shop/logic/low-high.ts`：~120 行购买骨架复制

- `buyClassicGood`（fes.ts:122-198）≡ `buyHighGood`（low-high.ts:99-170），差异仅 shop key（`"CLASSIC"` vs `"HS"`）与局部变量名
- 池选择器 4 连：`currentLimitedPool`(fes.ts:256-266) / `_currentStandardPool`(low-high.ts:222-232) / `_currentClassicPool`(240-250) / `_currentFesClassicPool`(259-266) —— 只有 `.filter()` 谓词不同
- 商品字面量 3 连：`buildHighCharGoods`(333-368) / `buildClassicCharGoods`(378-424) / `_buildFesPickGood`(fes.ts:390-414)
- 列表合并 2 连：`buildHighGoodList`(461-474) / `buildClassicGoodList`(480-493)

`shop/logic/` 下没有 `shared.ts`；同文件的 `_assertAffordable`/`_assertAvail`/`_issueCharItem` 已收敛在 `misc.ts`/`low-high.ts`，说明拆分意图已建立但购买骨架没抽。

### 🟠 P1-3 `time.ts` 三个格式化 helper 从未落地，78 处内联副本仍在

`app/core/utils/time.ts:84-129` 新增的 `formatDateCompact` / `formatCompactTimestamp` / `formatTimestamp` / `daysSince` 的 docstring 自称「收敛此前手写 padStart 句式 ×6」「收敛 medal.ts 中逐字重复 ×78 的注册天数表达式」，但实测：

| helper | 生产调用方 | 测试调用方 | 与此同时的内联副本 |
|---|---|---|---|
| `formatDateCompact` | 0 | 0 | `logger.ts:36`、`log-service.ts:84`、`capture-manager.ts:294`、`AdminService.ts:266`、`shop/logic/social.ts:35`、`index.ts:28` |
| `formatTimestamp` | 0 | 0 | 同上（各文件内 `padStart` 句式） |
| `formatCompactTimestamp` | 0 | 0 | `AdminService.ts` 备份文件名段 |
| `daysSince` | 0 | 0 | `medal.ts` **71 处** `moment().diff(moment(...),"days")`、`checkin/openServer.ts:1` 处 |

即：工具函数是死代码，重复实现一行未减。对应旧清单 §二.16/17 与批次 3「小工具批」，尚未执行。

### 🟠 P1-4 两个 ops 单例生命周期骨架逐字重复（~62 行）

`ops/assets/asset-registry/asset-service.ts` 与 `ops/capture/capture-manager.ts`：

| 成员 | asset-service | capture-manager |
|---|---|---|
| `configure()` | 157-164 | 205-212 |
| `get root()` | 167-169 | 215-217 |
| `isReady()` | 172-174 | 225-227 |
| `init()`/`ensureInit()` | 180-189 | 233-244 |
| `close()` | 192-201 | 247-256（**逐字相同**） |
| `reset()` | 204-208 | 259-263 |
| `subscribe()` | 211-214 | 271-274 |
| `emit()` | 217-225 | 276-284 |

配套的 `asset-db.ts:67-73 #openAssetDb` ≡ `capture-db.ts:68-74 #openCaptureDb` 也逐字相同（旧清单 §二.19 提过 `openSqlite`，仍未落地）。

### 🟠 P1-5 roguelike `SCENE` 事件构造惯用法 ×8（~110 行）

同一段 `choices`/`choiceAdditional` 双 reduce + `rlv2:event:create` "SCENE" 载荷在 8 处手写：`battle-nav.ts:73-87`、`battle-nav.ts:162-178`、`grid-nav.ts:330-346`、`grid-nav.ts:490-506`、`grid-nav.ts:535-551`、`incident.ts:174-187`、`logic.ts:399-416`、`event.ts:607-619`（其中 `event.ts` 的 `buildSceneChoices` 是未导出闭包，其余 7 处各写各的）。另有 `battle-nav.ts:181-192` ≡ `194-205` 的领取奖励 11 行重复。

### 🟡 P2 其余重复（量级较大但机械）

| 项 | 位置 | 量 |
|---|---|---|
| mission 模板 `init` 样板 | `mission/templates/{building,char,economy,stage,rlv2,arkhub}.ts` | `mission.progress.push({...})` ×131，其中 57 次为同一 `parseInt(mission.param[1])` 句式；文件内逐字重复分支 18 组（如 `building.ts:69-79 ≡ 80-90`） |
| admin 路由错误样板 | `ops/admin/admin-router.ts` 等 | `res.status(400).json({ error: (err as Error).message })` ×77 |
| 路由尾样板 | app 全域 | `res.send(player.delta satisfies X)` ×182；`req.body as X;` 空断言 ×589 |
| 深拷贝惯用法 | `roguelike/PlayerStatus/AccountManager/freshPlayer` 等 | `JSON.parse(JSON.stringify(` ×11（`delta.ts` 已有更健壮的 cloneData 未导出） |
| scripts 工具副本 | `scripts/` | `readBundleBytes` ×5、`collectLuaAssets` ×2、`transName` ×3、`bundleToModName` ×2、`downloadAnon`/`ensureDownloaded` 各 1（旧清单 §二.20-26 的 `scripts/lua/bundle-io.ts` 未落地） |
| building 委派返回类型重写 | `building/logic.ts:868-954` ↔ `logic/misc.ts:260-268/417-428` | 同一 7 字段对象形状出现 6 份，访客记录形状 3 份 |

---

## 二、多余代码（死代码 / 无用表面）

### 2.1 零引用文件（11 个）

| 文件 | 大小 | 说明 |
|---|---|---|
| `app/game/modules/account/user.model.ts` | **0 字节** | 空文件 |
| `app/game/modules/charm/charm.ts` | 282 B | 只有 JSDoc，无任何代码（`charm/routes.ts:5` 注释里还引用它） |
| `app/game/modules/activities/shared/activity.model.ts` | 90 行 | 手写 `PlayerActivity` + namespace，全仓零引用；权威定义在生成的 `excel/types-playerdata.ts:473` |
| `app/game/modules/autochess/public.ts` | 15 行 | 无跨模块调用方（文件注释自认） |
| `app/game/modules/{building,gacha,mission,roguelike,shop}/index.ts` | 各 14-16 行 | 5 个 barrel，全仓零引用（无目录式 import） |
| `app/game/modules/{gacha,roguelike}/trigger.ts` | 各 9-10 行 | 空实现的占位函数（`registerGachaTriggers`/`registerRlv2Triggers`），唯一导出零引用 |

> 注：`app/game/excel/types-playerdata.ts` 表面上「零引用」，实际经 `kernel/playerdata.ts:11 export * from "@excel/types-playerdata"` 被大量使用，**不是**死文件（我的首轮检测脚本漏了 `export *` 边，已修正）。

### 2.2 零引用导出

- **483 个**业务导出（非 excel）零引用，分布前三：`roguelike/rlv2-model.ts` 96、`ops/proxy/index.ts` 28、`ops/admin/AdminService.ts` 16。
  - `ops/proxy/index.ts` 的 28 个是 barrel re-export：消费者（如 `forwarder.ts:27`）直接 import `./transform`，barrel 本身无人使用。
  - `rlv2-model.ts` 的 96 个多为「为对齐官方 CS 字段而声明的接口」，属**有意保留的类型文档**，建议标注 `@internal` 或加白名单，而不是删除。
- **291 个** `excel.ts` re-export 未被业务使用。该文件是刻意的防腐层（`excel.ts:1876-1888` 说明「业务层唯一类型入口」），属**有意设计**，建议降级（真的没被引用的可以删，但优先级最低）。
- **155 个**仅被测试引用的导出，其中含刻意测试钩子（`arkpixel.ts` 的 `_resetPendingPixelUploadsForTest`、`setPixelsDirForTest`）——非问题，但建议统一命名并加 `@internal`。

### 2.3 未使用 import 绑定 4,001 个

app 3,966（103 个文件）＋ tests 35（22 个文件）。分布：activities 3,588 / building 133 / roguelike 77 / sandbox 53 / excel.ts 17。成因见 P1-1：ESLint 规则已配置但**无 lint 脚本**，`tsc` 默认不报未使用导入。

### 2.4 孤儿 zod schema 18 个（其中 15 个对应未校验路由）

`validateBody` 守卫（`tests/unit/architecture/schema-first-guard.test.ts`）只扫 `routes.ts` / `*.routes.ts` / `plugin-heartbeat.ts`（见该文件 31 行 `ROUTER_FACE`，且 29-30 行注释自认排除 `handler.ts`）。实测：

| 文件 | POST 路由 | 未经 validateBody |
|---|---|---|
| `app/game/modules/building/handler.ts` | 65 | **16**（207/301/322/340/356/368/388/396/415/468/530/602/652/704/726/737） |
| `app/game/modules/gacha/handler.ts` | 13 | 1（261） |
| `app/core/auth/auth.ts` | 33 | 33 |
| `app/ops/admin/official-ops.ts` | 7 | 7 |
| 守卫覆盖面（routes.ts 等 33 文件） | 378 | 0 ✅ |

`building/schemas.ts` 恰好有 **15 个零引用 schema**，且名字与上述 16 条未校验路由一一对应：

```
accelerateSolutionSchema   ↔ /building/accelerateSolution      (handler.ts:340)
buildRoomSchema            ↔ /building/buildRoom               (207)
changeDiySolutionSchema    ↔ /building/changeDiySolution       (415)
changeManufactureSolutionSchema ↔ /building/changeManufactureSolution (396)
confirmPrivateDormIntimacySchema ↔ /building/confirmPrivateDormIntimacy (322)
deleteOrderSchema / deliveryBatchOrderSchema / gainAllIntimacySchema /
getInfoShareRewardSchema / getThumbnailUrlSchema / receiveClueToStockSchema /
saveDiyPresetSolutionSchema / sendEmojiSchema / settleSaleSchema / takeClueFromBoardSchema
```

即契约已写好但没接线（唯一例外 `/building/getMessageBoardContent` 无 schema）。

> 修复：15 个 schema 接上 `validateBody`（顺带把守卫的 `ROUTER_FACE` 扩到 `handler.ts`），或删除 schema 并接受现状。二者都必须选一个，现状是「schema 与守卫同时假装覆盖」。

---

## 三、多余测试与测试重复

### 3.1 一个 100% 重复的测试文件（37 个用例跑两遍）

`tests/unit/game/model/events.test.ts` 与 `tests/unit/model/events.test.ts`：534 / 535 行、37 个 `it`（49 个 describe+it 标题全部相同）、51 个 `expect`，`Compare-Object -SyncWindow 0` **0 处差异**（唯一差异是末尾换行），两者 import 的都是 `@game/kernel/events/runtime`。

删哪一份有 git 依据：`app/model/` 在 826 个提交中**从未存在**，而 `app/game/model/` 确实存在过（`app/game/model/character.ts` 等，在 `dcb8566` 被删）—— 因此保留 `tests/unit/game/model/events.test.ts`，**删除 `tests/unit/model/events.test.ts`**。两者其实都已过期：现存模块是 `app/game/kernel/events/runtime.ts`，正确落点是 `tests/unit/game/kernel/events/runtime.test.ts`（同类先例：`tests/unit/game/modules/activities/arkhub/gateway/*.test.ts`）。

### 3.2 最大单点：`mockExcel` helper 零使用，122 个文件各写一份 excel 门面（4,762 行）

`tests/helpers/mockExcel.ts`（98 行，内含与 `excel.ts` 一致的 `getItem/itemName/makeItem/charData/stageData` 门面）**没有被任何一个测试文件 import**（只有 `tests/helpers/index.ts` 的 re-export 与它自己的 `mocks.test.ts`）。与之对照：

| 指标 | 实测 |
|---|---|
| 手写 `vi.mock("@excel/excel", ...)` 门面的测试文件 | **122 个 / 4,762 行** |
| 其中出现逐字相同 `getItem(id: string) { return this.ItemTable?.items?.[id]; }` 的文件 | **116 个** |
| 其中出现逐字相同门面注释「excel 门面方法（与 excel.ts 实现一致，操作 mock 数据）」的文件 | 105 个 |
| 单文件最大手写门面 | `manager/char.test.ts` 206 行、`manager/equipmentMission.test.ts` 175 行、`modules/mission/mission-special-operator.test.ts` 171 行 |

`mockEventBus` 同样形同虚设（仅 3 个文件提及，其中 2 个是 helper 自身），而 `mockTypedEventEmitter` 有 38 个文件在用。

> 修复：`vi.mock("@excel/excel", () => ({ default: mockExcel() }))`，按测试需要扩展 `MockExcel` 的表；122 个文件的 4,762 行门面可回收绝大部分。

### 3.3 其余测试脚手架重复（按量排序）

| 重复块 | 规模 | 建议落点 |
|---|---|---|
| `makePlayer()` 工厂 + `mockPlayerData({pushFlags,rlv2,medal,mission})` 引导 | **57 文件 / 1,066 行**；`medal: { medals:{}, custom:{ currentIndex:"0", customs:{} } }` 一行在 **53** 个文件逐字出现；`rlv2-battle-finish-data:62-72 ≡ rlv2-battle-reward:56-66` 字节相同 | `tests/helpers/playerManager.ts`、`tests/helpers/rlv2Player.ts` |
| 路由单元测试脚手架 `mockRes()` / `call()` | `mockRes` **48 文件 / 341 行**（其中 28 份字节相同）；`call()` 28 份字节相同（合 76 文件） | `tests/helpers/routeHarness.ts`（`apiServer.ts` 只覆盖 HTTP 集成形态） |
| building 集群夹具（14 个测试文件） | 58 行在 ≥8 个文件重复（702 处行出现）：`vi.hoisted(BuildingData…)`、`mockPlayer.update` 覆写、`vi.mock("@game/kernel/PlayerDataManager")` 出现 **14** 次 | `tests/helpers/buildingPlayer.ts` |
| 时间/Random 打桩 | `vi.mock("@utils/time")` **55 文件**（其中 ≥9 另带 `vi.hoisted`）、`vi.mock("moment")` 6 文件、`vi.spyOn(Math,"random")` **31 文件 / 96 处** | `tests/helpers/timeMock.ts`、`tests/helpers/random.ts` |
| 目录重名 | `tests/unit/util/`（2 文件）与 `tests/unit/utils/`（9 文件）并存；前者镜像的 `app/util/` 从未存在 | 合并到 `tests/unit/utils/` |

### 3.4 自证式模型测试（约 1,500 行，建议删除或改为类型夹具）

`tests/unit/model/`（5 文件，2,232 行）里有 3 个文件断言的全部是**自己刚构造的字面量**，不调用任何 `app/` 运行时代码：

| 文件 | 行 / 用例 | 问题 |
|---|---|---|
| `model/character.test.ts` | 489 / 22 用例（77 断言） | `:2-21` 从 `@game/modules/character` 导入 18 个类型，而该模块**不存在**（目录下没有 `index.ts`/`public.ts`）；77/77 断言为本地字面量自证 |
| `model/playerdata.test.ts` | 633 / 28 用例（77 断言） | 34 个类型导入中 **9 个已被重命名消失**（`PlayerGachaPool`→`PlayerGacha_PlayerGachaPool`、`PlayerNewbeeGachaPool`→`PlayerGacha_PlayerNewbeeGachaPool`、`PlayerFreeLimitGacha`/`PlayerAttainGacha`/`PlayerSingleGacha`/`PlayerFesClassicGacha`/`PlayerDataShop`/`MissionPlayerState`/`BaseProgress` 同类）；76/77 断言自证；仅 `:542-558` 真调 `mockPlayerData` |
| `model/battle.test.ts` | 434 / 14 用例（46 断言） | 45/46 断言自证（导入本身有效，属判断题：删或改为 `expectTypeOf` 类型夹具） |

**根因**：`tsconfig.json` 的 `include` 只有 `["app/**/*", "index.ts"]`，`vitest.config.mts` 也没开 `test.typecheck` —— 276 个测试文件、64,595 行**从不做类型检查**；而 `import type` 在转换阶段被擦除，所以「导入不存在的模块」也能长久绿灯。

> 建议：① 删除或改造这 3 个文件（保留 `:542-558` 那 2 个真实断言，移入 `tests/helpers/mocks.test.ts`）；② 增设 `tsconfig.test.json` 把 `tests/**/*` 纳入 `pnpm run build`（或给 vitest 开 `typecheck`），此后同类腐化会立即暴露。

### 3.5 残留与死角

- `tests/unit/__probe__.test.ts`（7 行）：`expect(1 + 1).toBe(2)`，vitest 配置探针残留，零覆盖 → 删。
- `tests/unit/router/crisis.test.ts:57-61`：`vi.mock("./crisis-seasons", …)` 相对测试文件解析到 `tests/unit/router/crisis-seasons.ts`，**该文件不存在**（真实模块是 `app/game/modules/crisis/crisis-seasons.ts`）；测试已改用 `@utils/file` 的 `readJson` 打桩，这个 mock 块是死的（未运行 vitest 验证是否硬失败，但目标路径确证不存在）。
- `it.skip` / `.todo` / `.only`：**0 处**（无僵尸测试）；无「零 it」文件。
- 断言密度：仅 1 个文件异常（`rlv2/rlv2-glade-gen.test.ts`，2 个 expect / 103 行）。

### 3.6 覆盖重叠：多数**不是**冗余（防误判）

59 个源模块被 2+ 测试文件覆盖（`kernel/PlayerDataManager.ts` 55、`excel/excel.ts` 27、`account/AccountManager.ts` 23）。逐个抽样后的结论：

| 疑似重复 | 裁决 |
|---|---|
| `manager/playerdata-medal.test.ts` ↔ `modules/mission/playerdata-mission.test.ts`（相似 0.38） | **可合并的唯一一对**：同一挂载路径 + 不同子管理器 + 仅 `toBeDefined()` 断言 → 合并为表驱动挂载测试，无覆盖损失 |
| `router/user-misc.test.ts` ↔ `router/user-status.test.ts`（0.36） | **保留**：分别打 `routes.ts` 导出的 `rootRouter`(:491) 与 `router`(:471)，端点不相交（前者 `/performanceStory/startStory`、`/share/confirmShareMission`；后者 `/buyAp`、`/exchangeDiamondShard`、`/useItem`） |
| `rlv2-battle-{finish-data,reward,chaos}`（0.43-0.50） | **保留三份**：分别测战报字段消费 / 收藏品掉落 / CHAOS 坍缩；重复的是 setup（`makePlayer` 字节相同）与 2 个仅 `toBeDefined()` 的 BATTLE_REWARD 断言（`finish-data:131-134,142-145`，已被 `reward:104-108` 覆盖） |
| `rlv2-glade-gen` ↔ `rlv2-shop-reach`（0.52，非 events 对里最高） | 保留：脚手架重叠 100%、断言重叠 0% |
| 9 个 `user-*` 路由测试 | 保留：8 个用 `rootRouter`、1 个用 `router`，端点集合互不相交 |

### 3.7 旧镜像目录（前提纠正）

- `app/manager/*`、`app/router/*` **在 826 个提交中从未存在**；实际被重构删除的是 `app/game/manager/`（33 文件）与 `app/game/router/`（35 文件），均在 `dcb8566`。因此 `tests/unit/manager/`(53) 与 `tests/unit/router/`(41) 镜像的是这两个目录。
- 94 个文件全部引用现存模块，**没有**「目标模块已删」的孤儿测试 → 不能以「源码没了」为由删除。
- 另有 3 个陈旧镜像目录：`tests/unit/model/`（镜像已删的 `app/game/model/`，含本报告唯一重复文件与 2 个自证式文件）、`tests/unit/util/`（`app/util/` 从未存在）、`tests/unit/game/router/`（1 个文件，实际目标是现存的 `modules/system/plugin-heartbeat`）。
- 58/94 个文件名已与任何现存 basename 无对应（14 个 `building-*`、`playerdata-medal`、`user-misc`、`home-misc`、`quest-assist` …）。改名/搬家属纯整理，AGENTS.md 已声明「按原镜像路径」维持现状，本报告不建议为此单独排期（若要动，随 3.2/3.3 的 helper 抽取一起做）。

---

## 四、核实后**不是**重复（防止误删）

| 疑似 | 结论 |
|---|---|
| `tests/unit/manager/*`、`tests/unit/router/*`、`tests/unit/model/*` | 镜像的是**已被 `dcb8566` 删除**的 `app/game/manager`、`app/game/router`、`app/game/model`（`app/manager`/`app/router` 从未存在）；94 个文件全部引用现存模块，内容各自有效（`tests/unit/model/` 例外，见 3.4） |
| activities 30 个 router 之间整文件相似度 0.63-0.90 | 主要是 128 行类型 import 块 + stub 惯用法；**handler 无复制**（各 `logic.ts` 内 `router.post(` 计数为 0，router 只做委派） |
| `mission/templates/*.ts` 5 个文件 | 92 个模板键**零跨文件冲突**，是域分解；重复的是 `init` 样板（见 P2） |
| `roguelike/event.ts` / `logic.ts` / `{events,map,troop}.ts` | 分区拆分 + 薄委派（`logic.ts:1111-1194`）+ 6 行拆分注释 ×9；语义不重复 |
| `excel.ts` 291 个未使用 re-export | 防腐层设计意图（`excel.ts:1876-1888`） |
| `rlv2-model.ts` 96 个未使用接口 | 官方 CS 字段对齐用的类型文档 |
| 54 条路由 URL 被 2+ 测试文件使用 | 抓包/proxy/auth/集成测试各自使用同一 URL 作 fixture，覆盖的是不同链路 |
| rlv2 测试簇（48 文件 / 11,714 行，占测试套件 18%）、`building-*`（14 文件） | 断言维度互不相同，重复的是 setup（见 3.3/3.6） |
| `SocialManager` vs `SocialService`、`BattleInfoStore`/`BattleStore`/`CrisisBattleStore`、三个 composition 文件 | 旧清单第六节已核实为分层/DI，维持现状 |
| `tests/unit/capture/*`、`tests/unit/db/*` | 各自覆盖独立层次 |

---

## 五、建议执行批次（按 收益/风险 排序）

### 批次 A：纯删除死代码（零风险，≈420 行，改完跑全量测试即可）
1. `interlockRefresh/logic.ts:150-329`（184 行，改用 `../shared/shared`）+ 清理同文件未用导入
2. `bossRush/router.ts:216-286` + `:57-72` 的 `EnemyDuel*` 类型 import（约 87 行）
3. `arkpixel.ts:262-297`（31 行）
4. `home/routes.ts:76-101` + `home/home.schema.ts:23-43` + `routes.ts:32-41` 跨模块 import + `module-boundary.test.ts:86` 白名单（约 40 行）
5. `account/user.model.ts`（0 字节）、`charm/charm.ts`（纯注释）、`activities/shared/activity.model.ts`（90 行）、`autochess/public.ts`
6. 5 个 `modules/*/index.ts` + 2 个 `*/trigger.ts` 占位（约 100 行）
7. `tests/unit/{game/model,model}/events.test.ts` 删 `model/` 那份（535 行，-37 用例；git 依据见 3.1）
8. `tests/unit/__probe__.test.ts`（7 行探针残留）
9. `tests/unit/router/crisis.test.ts:57-61` 死 `vi.mock("./crisis-seasons")` 块

### 批次 B：导入裁剪（机械、可 lint 自动化，≈3,500 行）
1. 在 `package.json` 增加 `"lint": "eslint app tests --fix"`，先跑一遍 `@typescript-eslint/no-unused-vars`（`eslint.config.mjs:9` 已启用）
2. 逐个修 `activities/` 30 个文件的类型 import（保留实际使用项），把 `shared/activity.ts` 按族拆薄
3. 顺手删除 tests 侧 35 个未用导入

### 批次 C：行为修复与守卫补齐
1. `fes.ts:221` 补 `_assertBuyCount(count)` + 负数量单测（安全相关）
2. `charRotation createPreset` 的 `instId` 契约定案（home 死副本 vs 生效副本已分叉）
3. building 15 个 schema 接线（并把 `schema-first-guard` 的 `ROUTER_FACE` 扩到 `handler.ts`），或删除 schema
4. `tsconfig.test.json` 纳入 tests，修 `tests/unit/model/character.test.ts` 的失效 import（并评估其 489 行自证式断言是否该删）
5. 测试 helper 收敛（收益巨大，建议单独一批，见批次 E）

### 批次 D：结构性收敛（旧清单剩余项）
1. `time.ts` 三个格式化 + `daysSince` 真正落地：替换 logger/log-service/capture-manager/AdminService/shop-social/index.ts 的 7 处内联与 `medal.ts` 的 71 处 `moment().diff`
2. `shop/logic/shared.ts`（抽 `buyProgressGood` / `resolvePool` / `buildCharGoods` 骨架）
3. `ops/shared/lazy-sqlite-store.ts`（`AssetRegistry`/`CaptureManager` 基类 + `openSqliteDb`）
4. roguelike `emitScene()` / `confirmReward()`（替换 8+2 处）
5. `mission/templates` 的 `progressInit()` 工厂（约 520 行样板）
6. `scripts/lua/bundle-io.ts`（`readBundleBytes`×5、`collectLuaAssets`×2、`transName`×3、并发池样板等）
7. 路由样板 `handle(schema, fn)` / `sendDelta(res, player)`（182 处 `player.delta satisfies` + 589 处空断言；注意 `player.delta` 单次读取约束）

### 批次 E：测试脚手架收敛（净减数千行，需逐文件跑测验证）
1. `tests/helpers/mockExcel.ts` 接入 122 个手写 excel 门面的文件（**4,762 行**，最大单点）
2. `tests/helpers/{routeHarness,playerManager,rlv2Player,buildingPlayer,timeMock,random}.ts` 抽取：`mockRes`(48)/`call`(28)/`makePlayer`(57)/building 夹具(14)/`vi.mock("@utils/time")`(55)/`Math.random` 打桩(31)
3. 合并 `playerdata-medal.test.ts` + `playerdata-mission.test.ts` 为表驱动挂载测试；删 `rlv2-battle-finish-data.test.ts:131-134,142-145` 两个仅 `toBeDefined()` 的断言
4. 处置 3 个自证式模型测试（`model/{character,playerdata,battle}.test.ts`，1,556 行）
5. 顺手归一化目录：`tests/unit/util/` → `tests/unit/utils/`，`tests/unit/game/router/plugin-heartbeat.test.ts` → `tests/unit/game/modules/system/`

---

## 附录：取证脚本与复跑命令

脚本位于 `tmp/audit/`（`tmp/` 已 gitignored，不入库）：

| 脚本 | 作用 | 复跑 |
|---|---|---|
| `deadcode.mjs` | 导入/导出图 + 零引用文件/导出（含 `export *` 传播、routes.ts 动态模块表） | `node tmp/audit/deadcode.mjs` |
| `dupes.mjs` | token 窗口克隆检测 + 整文件相似度 | `node tmp/audit/dupes.mjs app 45 2` |
| `unusedimports.mjs` | 未使用导入绑定 | `node tmp/audit/unusedimports.mjs app` |
| `testmap.mjs` | 测试↔源码映射、覆盖重叠、断言密度 | `node tmp/audit/testmap.mjs` |
| `testhelpers.mjs` | 测试内重复 helper（同名同体） | `node tmp/audit/testhelpers.mjs` |
| `duptitles.mjs` | 跨文件重复的 `it()` 标题 | `node tmp/audit/duptitles.mjs` |
| `routeoverlap.mjs` | 同一路由 URL 被多测试文件覆盖 | `node tmp/audit/routeoverlap.mjs` |
| `validatebody.mjs` | POST 路由 validateBody 覆盖率（按守卫覆盖面拆分） | `node tmp/audit/validatebody.mjs` |
| `orphantests.mjs` | 测试中无法解析的 import | `node tmp/audit/orphantests.mjs` |
| `duphelpers.mjs` | app 内同名同体局部惯用法 | `node tmp/audit/duphelpers.mjs` |
| `excelmock.mjs` | 测试内手写 `@excel/excel` 门面的文件数与总行数 | `node tmp/audit/excelmock.mjs` |

产物：`deadcode.txt`、`dead-non-excel.txt`、`testonly.txt`、`dupes-{app,tests,all}.txt`、`unused-{app,tests}.txt`、`testmap.txt`、`testhelpers.txt`、`duptitles.txt`、`routeoverlap.txt`、`validatebody.txt`。

> 顺带一提：`tmp/audit/` 里还堆着上一次会话遗留的约 230 个一次性分析脚本（`crisis-*.mjs`、`vhalfidle-*.mjs`、`medal-*.mjs` …）。`tmp/` 已 gitignored，不影响仓库，但需要时可一并清理。

验证建议：批次 A/B 后跑 `pnpm exec tsc --noEmit` + `pnpm exec vitest run`；基线为 HEAD（旧清单记录 2407 passed / 5 既有失败），新增失败即为回归。
